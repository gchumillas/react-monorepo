/**
 * Entry point.
 * 
 * Export the default component, functions and relevant types to the rest of
 * the application.
 */
export { default } from './Tabs'
export type { TabsItemProps, TabsProps } from './types'
